<h1>Você deseja:</h1>
<%= form_for :file_system do |f| %>
  <%= f.select :parent_id, options_for_select(nested_set_options(FileSystem) {|i| "#{'--' * i.level} #{i.name}" } ), prompt: 'Diretório Raiz' %>
  as ações serão refletidas aqui
  <hr />
  <%=f.label :name, 'Criar diretório com nome...' %>
  <%= f.text_field :name, required: true %>
  <br />
  <%= f.label :files, 'e/ou envie arquivos também' %>
  <%= f.file_field :files, multiple: true %>
  <br />
  <%= f.submit 'Salvar' %>
<% end %>
